#include <stdio.h>

int main(int argc, char **argv)
{
	char name[225];

	printf("Enter your name: ");
	fgets(nane, 255, stdin);
	printf("length = %d\n", strlen(name)); /* debug line */
	name[strlen(name)-1]= '\0';/* remove the newline at the end */

        printf("Hello world!\n", name);
        return 0;
}